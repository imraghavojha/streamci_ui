{
    "framework": "nextjs",
    "buildCommand": "npm run build",
    "installCommand": "npm install --legacy-peer-deps",
    "outputDirectory": ".next",
    "cleanUrls": true,
    "functions": {
        "app/**/*.tsx": {
            "maxDuration": 30
        }
    }
}